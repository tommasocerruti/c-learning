#include <stdio.h>
#define MAXL 30
int main() {
    FILE *fin,*fout;
    printf("Input the name of the file to read from:");
    scanf("%s",namein);
    printf("Input the name of the file to write to:");
    scanf("%s",nameout);
    filein="../%s";
    if (fopen(fin,"../%s"))
    return 0;
}
